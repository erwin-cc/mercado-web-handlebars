<div class="container text-center">
    <div class="row">
        <h3 class="fw-bold text-center py-4">Bienvenidos al mercado WEB, seleccione sus productos.</h3>
    </div>
    <div id="productos" class="mt-5">
        {{#each productos as |productos3|}}
        <div class="row gx-6 mt-3">
            {{#each productos3}}
                {{> producto}}
            {{/each}}
        </div>
        {{/each}}
    </div>
</div>